/**
 * Created by yanfaPC on 2017/6/20.
 */
(function () {
    //var value = Math.sqrt(Math.pow(a0 - a1, 2) - Math.pow(b0 - b2, 2));
    var value = Math.sqrt(Math.pow(10 - 5, 2) + Math.pow(3 - 6, 2));

    console.log(value);
    document.getElementById('logdiv').innerHTML = value;
})();
